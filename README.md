# docker-testapp# docker-p1

it just a simple log in app where it will ask for email, username, pass and using docker we will set up container to save our data in the mongo database and get an user interface using the container mongo-express.
i was successfull able to set the docker.
